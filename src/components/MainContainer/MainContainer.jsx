import './MainContainer.scss';

const MainContainer = () => {
  return (
    <main>
      ici le corps de la page de base "visiteur" = vide ou logo Appli ou
      Photo/Logo de l'étabissment
    </main>
  );
};

export default MainContainer;
